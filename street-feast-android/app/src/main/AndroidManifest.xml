<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".StreetFeastApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StreetFeast">

        <!-- AuthGate Activity (Launcher - Single Router) -->
        <activity
            android:name=".activities.AuthGateActivity"
            android:exported="true"
            android:theme="@style/Theme.StreetFeast">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Splash Activity (Optional - can be removed) -->
        <activity
            android:name=".activities.SplashActivity"
            android:exported="false"
            android:theme="@style/Theme.StreetFeast" />

        <!-- Login Activity -->
        <activity android:name=".activities.LoginActivity" android:exported="false" />

        <!-- Main Activity (Waiter Activity) -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="false">
            <!-- Deep link for notification click-through (backward compatibility) -->
            <intent-filter>
                <action android:name="OPEN_ORDER" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Chef Page Activity -->
        <activity
            android:name=".activities.ChefPageActivity"
            android:launchMode="singleTask"
            android:exported="false">
            <!-- Deep link for notification click-through -->
            <intent-filter>
                <action android:name="OPEN_ORDER" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Waiter Activity -->
        <activity
            android:name=".activities.WaiterActivity"
            android:launchMode="singleTask"
            android:exported="false">
            <!-- Deep link for notification click-through -->
            <intent-filter>
                <action android:name="OPEN_ORDER" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
    </application>

</manifest>
